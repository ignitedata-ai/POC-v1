{
  "name": "tcs-poc-template-repository",
  "version": "1.0.0",
  "description": "TCS Plan of Correction - F-Tag Template Repository POC",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently --names server,client --prefix-colors blue,green \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
